# Frontend (Next.js)

## Run
```bash
npm install
npm run dev
```

## 环境变量
- 可选：把 `.env.local.example` 复制成 `.env.local`
- `NEXT_PUBLIC_API_BASE_URL`:
  - 本地开发时可填 `http://localhost:8000`
  - 如果前后端部署在同一域名并用 Nginx 反代，可以留空

## 页面
- `/`: 天津大学北洋园版校园美食推荐主页
